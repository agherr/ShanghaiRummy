{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "20",
    "pnpm": "9"
  },
  "steps": {
    "install": {
      "commands": [
        { "cmd": "cd .. && pnpm install --frozen-lockfile", "customName": "Install dependencies" }
      ]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": [
        { "cmd": "cd .. && pnpm --filter @shanghairummy/shared build", "customName": "Build shared package" },
        { "cmd": "cd .. && pnpm --filter @shanghairummy/client build", "customName": "Build client" }
      ]
    }
  },
  "deploy": {
    "inputs": [{ "step": "build", "include": ["."] }],
    "startCommand": "npx serve -s dist -l 3000"
  }
}
