{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "20",
    "pnpm": "9"
  },
  "steps": {
    "install": {
      "commands": [
        { "cmd": "pnpm install --frozen-lockfile -C ..", "customName": "Install dependencies" }
      ]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": [
        { "cmd": "pnpm --filter @shanghairummy/shared build -C ..", "customName": "Build shared package" },
        { "cmd": "pnpm --filter @shanghairummy/client build -C ..", "customName": "Build client" }
      ]
    }
  },
  "deploy": {
    "inputs": [{ "step": "build", "include": ["."] }],
    "startCommand": "npx serve -s dist -l 3000"
  }
}
