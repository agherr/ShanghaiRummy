{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "20",
    "pnpm": "9"
  },
  "steps": {
    "install": {
      "inputs": [{ "local": true, "include": ["."] }],
      "commands": [
        { "cmd": "pnpm install --frozen-lockfile", "customName": "Install dependencies" }
      ]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": [
        { "cmd": "pnpm --filter @shanghairummy/shared build", "customName": "Build shared package" },
        { "cmd": "pnpm --filter @shanghairummy/server build", "customName": "Build server" }
      ]
    }
  },
  "deploy": {
    "inputs": [{ "step": "build", "include": ["server"] }],
    "startCommand": "node server/dist/index.js"
  }
}
